name: Deploy Bookstore

on:
  push:
    branches: [main]

env:
  AWS_REGION: us-west-2
  STACK_NAME: bookstore-stack
  FUNCTION_NAME: GetBooks
  S3_BUCKET: bookstore-demo-918183255158

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy CloudFormation stack
        if: ${{ github.event_name == 'push' && contains(join(github.event.commits.*.modified, ','), 'bookstore-stack.yaml') || contains(join(github.event.commits.*.added, ','), 'bookstore-stack.yaml') }}
        run: |
          aws cloudformation deploy \
            --template-file bookstore-stack.yaml \
            --stack-name $STACK_NAME \
            --capabilities CAPABILITY_NAMED_IAM \
            --no-fail-on-empty-changeset

      - name: Package and deploy Lambda
        if: ${{ contains(join(github.event.commits.*.modified, ','), 'lambda_function.py') || contains(join(github.event.commits.*.added, ','), 'lambda_function.py') }}
        run: |
          zip function.zip lambda_function.py
          aws lambda update-function-code \
            --function-name $FUNCTION_NAME \
            --zip-file fileb://function.zip

      - name: Sync frontend to S3
        if: ${{ contains(join(github.event.commits.*.modified, ','), 'index.html') || contains(join(github.event.commits.*.added, ','), 'index.html') }}
        run: aws s3 cp index.html s3://$S3_BUCKET/index.html
